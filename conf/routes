# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Start.index
GET     /dashboard                              Dashboard.index
GET     /admin                                  Admin.index
GET     /about                                  About.index
GET     /settings                               Settings.index
POST    /settings/setFirstName                  Profile.setName
POST    /settings/setEmail                      Profile.setEmail
POST    /settings/setAddress                    Profile.setAddress
POST    /settings/setGender                     Profile.setGender
POST    /settings/setHeight                     Profile.setHeight
POST    /settings/setWeight                     Profile.setWeight
POST    /dashboard/addAssessment                Dashboard.addAssessment
POST    /dashboard/addTodo                      Dashboard.addTodo
GET     /dashboard/{id}/deleteAssessment        Dashboard.deleteAssessment
GET     /dashboard/{id}/deleteTodo/{todoid}     Dashboard.deleteTodo

GET    /signup                                  Accounts.signup
GET    /login                                   Accounts.login
GET    /logout                                  Accounts.logout

POST   /register                                Accounts.register
POST   /authenticate                            Accounts.authenticate


# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
